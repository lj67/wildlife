import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ImageTaggerPage } from './image-tagger';

@NgModule({
  declarations: [
  ],
  imports: [
    IonicPageModule.forChild(ImageTaggerPage),
  ],
})
export class ImageTaggerPageModule {}
